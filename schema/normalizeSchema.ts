import {MoldSchema} from '../interfaces/MoldSchema';


export function normalizeSchema (rawSchema: MoldSchema): MoldSchema {
  // TODO: преобразование упрощенной схемы в полную
  // TODO: валидация схемы
  // TODO: добавить параметр __normalized: true - и если он есть то ничего не делать.

  return rawSchema;
}
