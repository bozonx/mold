export default class ListInstance {
  constructor(root) {
    this._root = root;
  }

  /**
   * Returns several elements
   * @param {object} query - like {page: 1}
   */
  filter(query) {
    // TODO: !!!
  }

  /**
   * Returns only one element
   * @param {object} query - like {page: 1}
   */
  find(query) {
    // TODO: !!!
  }
  
  add(item) {
    // TODO: !!!
  }
  
  remove(item) {
    // TODO: !!!
  }
}
