
export default class _Mold {
  constructor(moldPath, action, schema) {
    this._moldPath = moldPath;
    this._action = action;
    this._schema = schema;
    this._state = undefined;
  }

  get state() {

  }

  init() {
    // TODO: берем корневой элемент и инициализируем его и берем у него mold
    //this._state;
  }

}
